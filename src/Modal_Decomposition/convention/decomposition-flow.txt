默认参数: chunk_size=SIZE["100MB"], safe=True, dtype=None, return_structure: Literal["None", "Flow", "Total"]="None"

1:  (鉴于该流程的高频性，可以在 coupling_flow (耦合-流程) 中新增 `_chunk_check.py`)
    1: safe = False, 如果 return_structure = None, 数据直接对接后续分解流程 | 否则, 分块后输出
       safe = True, 数据先在 Chunk 中分块后交给 Check 进行检测

    2: dtype = None, 保持数据本身的 dtype，否则进行转换

    3: return_type = None 不做处理 | Flow 返回流式输出 | Total 无论是否分块都需要保持返回为整体

2: 开始进行分解